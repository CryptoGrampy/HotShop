import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hotshop.app',
  appName: 'HotShop',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
